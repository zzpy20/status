{
  "name": "status",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:remote": "wrangler d1 migrations apply status-uptime --remote",
    "migrate:local": "wrangler d1 migrations apply status-uptime --local"
  },
  "devDependencies": {
    "wrangler": "^4.84.1"
  }
}
